"""Lifecycle commands for variant management.

Phase 1 (REG / CLI-01 / CLI-02 / CLI-05 / CLI-06 / CLI-08) will populate this
module with: apply, revert-baseline, port-variant, promote-variant,
refresh-registry.

Phase 0 ships an empty stub so Phase 1 has somewhere to land without touching
``cli/__init__.py`` structurally (D-01).
"""
from __future__ import annotations

# Phase 1: command registrations land here.
