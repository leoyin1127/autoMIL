# Learnings

Persistent knowledge base for the autoMIL experiment loop.
Consolidated sections at top are read FIRST on every session restart.
Per-experiment entries are appended below.

## What Works

(Populated after experiments. Agent consolidates patterns here every 5 experiments.)

## What Doesn't Work

(Populated after experiments. Agent consolidates anti-patterns here every 5 experiments.)

## Patterns and Heuristics

(Populated after experiments. General observations about the task, data, or models.)

## Experiment Log

(Agent appends timestamped entries here after each experiment.)
