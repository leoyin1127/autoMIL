---
phase: 04-6h-per-cell-hard-cap-cell-concept-formalisation
plan: 08
type: execute
wave: 1
depends_on: []

files_modified:
  - benchmarks/src/autobench/pipeline/clam/runner.py
  - benchmarks/scripts/run_experiment.py
  - tests/test_per_fold_writer.py
autonomous: true
requirements: [CAP-03]
tags: [autobench, ccrcc, fold-writer, sigterm, runner]

must_haves:
  truths:
    - "After fold_results.append(result) inside autobench/pipeline/clam/runner.py:run_experiment(), a fold_<i>_result.json file is written to AUTOMIL_RESULTS_DIR with the D-118 shape (fold_index, fold_count, status='completed', metrics, composite, elapsed_seconds, peak_vram_mb)."
    - "The fold-writer is gated on AUTOMIL_RESULTS_DIR being set — if unset (e.g. running outside the autoMIL orchestrator), the writer is a no-op."
    - "metrics keys map test_metrics + val_metrics from compute_extended_metrics() to flat val_auc, val_bacc, test_auc, test_bacc per Pitfall 5."
    - "benchmarks/scripts/run_experiment.py calls automil.runtime_helpers.register_sigterm_flush() at the top of main() BEFORE any DataLoader / multiprocessing initialisation."
    - "fold_count is read from os.environ.get('AUTOMIL_FOLD_COUNT', '5') — same source as runtime_helpers."
  artifacts:
    - path: benchmarks/src/autobench/pipeline/clam/runner.py
      provides: "Per-fold result file writer integration"
      contains: "fold_<i>_result.json, AUTOMIL_RESULTS_DIR, fold_index, fold_count"
    - path: benchmarks/scripts/run_experiment.py
      provides: "register_sigterm_flush() opt-in call"
      contains: "register_sigterm_flush"
    - path: tests/test_per_fold_writer.py
      provides: "Unit tests for the per-fold writer behaviour"
      contains: "test_writes_fold_file_when_results_dir_set, test_noop_when_results_dir_unset, test_metric_keys_mapped_correctly, test_writes_one_file_per_fold"
  key_links:
    - from: "benchmarks/src/autobench/pipeline/clam/runner.py"
      to: "AUTOMIL_RESULTS_DIR/fold_<i>_result.json"
      via: "explicit json.dump after fold_results.append"
      pattern: "fold_\\$\\{fold_index\\}_result\\.json|fold_.*_result\\.json"
    - from: "benchmarks/scripts/run_experiment.py:main"
      to: "automil.runtime_helpers.register_sigterm_flush"
      via: "early call before DataLoader construction"
      pattern: "register_sigterm_flush\\(\\)"
---

<objective>
Make the CCRCC training script fold-checkpoint-aware. After each fold completes inside `runner.run_experiment()`, write a `fold_<i>_result.json` file to `AUTOMIL_RESULTS_DIR` with the D-118 shape. Have the entry-point `run_experiment.py` call `register_sigterm_flush()` at startup so cap-driven cancels produce a clean, partial `result.json` via the framework's lazy-import handler.

Purpose: CAP-03 is impossible without per-fold files on disk. This plan is the autobench-side glue; it is consumer-supplied (autobench is one consumer of autoMIL). The framework owns the contract (D-118 shape + register_sigterm_flush API); autobench owns the call.

Output: 2 modified consumer files + 1 unit test. The framework files (cells/, runtime_helpers.py) are untouched here. NO autobench imports leak back into src/automil/ — this plan only touches benchmarks/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-PATTERNS.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md

<interfaces>
From .planning/phases/04-…/04-CONTEXT.md (D-118 fold file shape):
  fold_<i>_result.json:
    fold_index: int
    fold_count: int
    status: "completed"
    metrics: {val_auc, val_bacc, test_auc, test_bacc}  # flat floats
    composite: float
    elapsed_seconds: int
    peak_vram_mb: int

From .planning/phases/04-…/04-RESEARCH.md (Pitfall 5):
  compute_extended_metrics() returns dicts where auc_roc may be a flat float OR a {"mean": ..., "std": ...} dict; implementer must inspect the runtime shape.
  The mapping (best-effort, from Pattern 5 in PATTERNS.md):
    test_metrics["auc_roc"] (or .["mean"]) -> metrics["test_auc"]
    test_metrics["balanced_accuracy"] (or .["mean"]) -> metrics["test_bacc"]
    val_metrics["auc_roc"] (or .["mean"]) -> metrics["val_auc"]
    val_metrics["balanced_accuracy"] (or .["mean"]) -> metrics["val_bacc"]

From src/automil/runtime_helpers.py (Plan 04-02):
  register_sigterm_flush(*, fold_count_env: str = "AUTOMIL_FOLD_COUNT") -> None
  # idempotent; must be called from main thread of main interpreter BEFORE DataLoaders.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-fold writer to benchmarks/src/autobench/pipeline/clam/runner.py</name>
  <files>benchmarks/src/autobench/pipeline/clam/runner.py</files>
  <read_first>
    - benchmarks/src/autobench/pipeline/clam/runner.py (entire file — find the fold loop, locate `fold_results.append(result)`, identify the local variable name for `result` and `fold` index)
    - benchmarks/src/autobench/pipeline/clam/train.py (read train_fold to confirm the return shape: especially what keys `result["test_metrics"]` and `result["val_metrics"]` contain at the per-fold level)
    - benchmarks/src/autobench/pipeline/evaluate.py (read compute_extended_metrics to confirm whether auc_roc is a flat float or a {"mean": ..., "std": ...} dict at the per-fold call site — Pitfall 5)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (D-118 exact shape)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md (Pattern 5 + Pitfall 5)
  </read_first>
  <action>
    Locate the fold loop inside `run_experiment()` (or the equivalent function name — confirm by reading). The pattern is roughly:

      fold_results = []
      for fold in range(n_folds):
          result = train_fold(fold, results_dir, ...)
          fold_results.append(result)
          # <-- INSERT HERE

    Insert the following block IMMEDIATELY AFTER the existing `fold_results.append(result)` line. Use a helper function defined at module top to keep the loop readable. Place this helper near the imports:

      def _write_fold_result_json(fold_index: int, result: dict) -> None:
          """Write archive/<node>/fold_<i>_result.json per autoMIL CAP-03 / D-118.

          No-op when AUTOMIL_RESULTS_DIR is unset (e.g., running outside the
          autoMIL orchestrator).

          Pitfall 5: compute_extended_metrics() may return auc_roc as either a
          flat float or a {"mean": ..., "std": ...} dict. Defensively unwrap.
          """
          import json, os
          from pathlib import Path

          results_dir = os.environ.get("AUTOMIL_RESULTS_DIR")
          if not results_dir:
              return  # not running under autoMIL orchestrator
          fold_count = int(os.environ.get("AUTOMIL_FOLD_COUNT", "5"))

          def _unwrap(metric):
              # auc_roc may be float OR {"mean": float, ...}
              if isinstance(metric, dict):
                  return float(metric.get("mean", 0.0))
              try:
                  return float(metric or 0.0)
              except (TypeError, ValueError):
                  return 0.0

          test_m = result.get("test_metrics", {}) or {}
          val_m = result.get("val_metrics", {}) or {}
          metrics = {
              "val_auc":   _unwrap(val_m.get("auc_roc")),
              "val_bacc":  _unwrap(val_m.get("balanced_accuracy")),
              "test_auc":  _unwrap(test_m.get("auc_roc")),
              "test_bacc": _unwrap(test_m.get("balanced_accuracy")),
          }
          composite = (metrics["test_auc"] + metrics["test_bacc"]) / 2.0

          payload = {
              "fold_index":      fold_index,
              "fold_count":      fold_count,
              "status":          "completed",
              "metrics":         metrics,
              "composite":       composite,
              "elapsed_seconds": int(result.get("elapsed_seconds", 0) or 0),
              "peak_vram_mb":    int(result.get("peak_vram_mb", 0) or 0),
          }
          fold_path = Path(results_dir) / f"fold_{fold_index}_result.json"
          fold_path.write_text(json.dumps(payload, indent=2))

    Then, immediately after `fold_results.append(result)` in the loop body, add:

          _write_fold_result_json(fold, result)

    Use the actual variable names from the source (likely `fold` for the fold index, `result` for the per-fold dict). If the loop uses different names, adapt accordingly — the action is "call _write_fold_result_json with the just-appended fold index and result".

    Do NOT change the existing `train_fold()` API. Do NOT alter `compute_confidence_intervals` or any other downstream aggregation — the framework's `aggregate_folds` will reconstruct the partial composite from these per-fold files independently.

    If results_dir variable name in run_experiment differs (e.g., `automil_results_dir`), make the helper read from os.environ.get("AUTOMIL_RESULTS_DIR") regardless — this keeps the helper decoupled from the runner's local variables (Pitfall 5 safety: env-driven, not arg-driven).
  </action>
  <verify>
    <automated>grep -c "_write_fold_result_json" benchmarks/src/autobench/pipeline/clam/runner.py && grep -c 'fold_.*_result.json\|f"fold_{' benchmarks/src/autobench/pipeline/clam/runner.py && grep -c "AUTOMIL_RESULTS_DIR" benchmarks/src/autobench/pipeline/clam/runner.py && uv run pytest benchmarks/tests/ -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _write_fold_result_json" benchmarks/src/autobench/pipeline/clam/runner.py` returns 1.
    - `grep -c "_write_fold_result_json(fold, result)\|_write_fold_result_json(.*, result)" benchmarks/src/autobench/pipeline/clam/runner.py` returns at least 1 (call site in fold loop).
    - `grep -c "AUTOMIL_RESULTS_DIR" benchmarks/src/autobench/pipeline/clam/runner.py` returns at least 2 (helper + value-check).
    - `grep -c "AUTOMIL_FOLD_COUNT" benchmarks/src/autobench/pipeline/clam/runner.py` returns at least 1.
    - `grep -c '"fold_index"' benchmarks/src/autobench/pipeline/clam/runner.py` returns at least 1.
    - `grep -c '"composite"' benchmarks/src/autobench/pipeline/clam/runner.py` returns at least 1.
    - `grep -c '"val_auc"' benchmarks/src/autobench/pipeline/clam/runner.py` returns at least 1.
    - `grep -c "def _unwrap" benchmarks/src/autobench/pipeline/clam/runner.py` returns 1 (Pitfall 5 defensive unwrap).
    - Existing autobench tests pass: `uv run pytest benchmarks/tests/ -x` succeeds (or returns clean if no autobench tests directory exists yet).
  </acceptance_criteria>
  <done>Per-fold result files are written immediately after each fold completes, gated on AUTOMIL_RESULTS_DIR; helper handles both flat-float and CI-dict metric shapes from compute_extended_metrics; runner.py untouched outside the additive insertion.</done>
</task>

<task type="auto">
  <name>Task 2: Add register_sigterm_flush() call to benchmarks/scripts/run_experiment.py</name>
  <files>benchmarks/scripts/run_experiment.py</files>
  <read_first>
    - benchmarks/scripts/run_experiment.py (entire file — locate main() entry, identify where DataLoaders / multiprocessing pools are constructed)
    - src/automil/runtime_helpers.py (Plan 04-02 — for the API)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md (Pitfall 1 — handler MUST install in main thread BEFORE DataLoader)
  </read_first>
  <action>
    Modify `benchmarks/scripts/run_experiment.py` in two places:

    1) Add an import at the top of the file (with the other imports, after stdlib but before autobench/local imports):

       from automil.runtime_helpers import register_sigterm_flush

    2) Insert the call at the VERY TOP of `main()` (or whatever the entry-point function is — usually called `main` or `run` or guarded by `if __name__ == "__main__"`). The call MUST happen before:
       - any `torch.utils.data.DataLoader(...)` construction
       - any `multiprocessing.Pool` or `multiprocessing.Process` instantiation
       - any `threading.Thread` start

       If main is structured like:

         def main():
             args = parse_args()
             # ... setup ...
             dataset = load_dataset(args)
             ...

       Insert as the first statement INSIDE main() (after argparse if argparse uses sys.exit on --help — argparse is fine, it doesn't fork):

         def main():
             # CAP-03 / D-121: install SIGTERM handler BEFORE any DataLoader/multiprocessing setup.
             # The handler aggregates archive/<node>/fold_*_result.json into a partial result.json
             # on cap-driven cancel, then sys.exit(0) — letting the orchestrator reconcile to
             # status='executed' with metadata.budget_killed=True.
             register_sigterm_flush()
             args = parse_args()
             ...

       If main() is launched via `if __name__ == "__main__": main()`, the call inside main() is sufficient. If the script does any module-level work that constructs DataLoaders, move that work INSIDE main() — the project pattern is to keep module-level minimal.

    3) Verify NO `os.environ.get("AUTOMIL_RUNTIME")` or other autobench-only env handling is removed. This task is purely additive.

    Run a smoke check: `uv run python -c "from benchmarks.scripts.run_experiment import main; print('OK')"` (or the equivalent import path; verify the file imports without error).
  </action>
  <verify>
    <automated>grep -c "from automil.runtime_helpers import register_sigterm_flush" benchmarks/scripts/run_experiment.py && grep -c "register_sigterm_flush()" benchmarks/scripts/run_experiment.py && uv run python -c "import benchmarks.scripts.run_experiment; print('IMPORT OK')" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from automil.runtime_helpers import register_sigterm_flush" benchmarks/scripts/run_experiment.py` returns 1.
    - `grep -c "register_sigterm_flush()" benchmarks/scripts/run_experiment.py` returns at least 1.
    - The line `register_sigterm_flush()` appears textually BEFORE any `DataLoader(` / `multiprocessing` / `Process(` / `Pool(` reference in the file. Verify with: `awk '/register_sigterm_flush\(\)/{r=NR} /DataLoader\(|multiprocessing\.|Pool\(|Process\(/{if(r>0 && NR<r){print "FAIL:line "NR" before register_sigterm_flush at line "r; exit 1}} END{print "OK"}' benchmarks/scripts/run_experiment.py` returns "OK" (or no DataLoader/pool references at all).
    - `uv run python -c "import benchmarks.scripts.run_experiment"` succeeds (no import error).
    - File compiles: `uv run python -m py_compile benchmarks/scripts/run_experiment.py` succeeds.
  </acceptance_criteria>
  <done>register_sigterm_flush() is called at the top of main() before any DataLoader / multiprocessing / threading construction; SIGTERM-driven flush is now wired end-to-end for autobench experiments.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests/test_per_fold_writer.py (unit tests for fold-writer)</name>
  <files>tests/test_per_fold_writer.py</files>
  <read_first>
    - benchmarks/src/autobench/pipeline/clam/runner.py (just-modified — for _write_fold_result_json signature)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (D-118 expected shape)
  </read_first>
  <action>
    Create `tests/test_per_fold_writer.py`. The helper `_write_fold_result_json` is defined inside `benchmarks.src.autobench.pipeline.clam.runner` so we import it directly (with sys.path setup if needed — autobench is a workspace package).

    Required tests:

    1. test_writes_fold_file_when_results_dir_set:
       - Set AUTOMIL_RESULTS_DIR to tmp_path via monkeypatch.setenv.
       - Call `_write_fold_result_json(2, {"test_metrics": {"auc_roc": 0.85, "balanced_accuracy": 0.82}, "val_metrics": {"auc_roc": 0.86, "balanced_accuracy": 0.81}, "elapsed_seconds": 100, "peak_vram_mb": 4500})`.
       - Assert `(tmp_path / "fold_2_result.json").exists()`.
       - Load the JSON; assert: fold_index==2, status=="completed", metrics["test_auc"]==0.85, metrics["val_auc"]==0.86, metrics["test_bacc"]==0.82, metrics["val_bacc"]==0.81, composite == pytest.approx((0.85+0.82)/2.0), elapsed_seconds==100, peak_vram_mb==4500.

    2. test_noop_when_results_dir_unset:
       - Use monkeypatch.delenv("AUTOMIL_RESULTS_DIR", raising=False).
       - Call `_write_fold_result_json(0, {...})`.
       - Assert no file is written under tmp_path. No exception raised.

    3. test_metric_keys_mapped_correctly_from_dict_shape:
       - Set AUTOMIL_RESULTS_DIR.
       - Call with `result["test_metrics"] = {"auc_roc": {"mean": 0.91, "std": 0.02}, "balanced_accuracy": {"mean": 0.88, "std": 0.01}}` (CI-dict shape per Pitfall 5).
       - Assert metrics["test_auc"] == pytest.approx(0.91); metrics["test_bacc"] == pytest.approx(0.88) — i.e. _unwrap handled the dict shape.

    4. test_writes_one_file_per_fold:
       - Call with fold_index 0, 1, 2, 3, 4 in succession.
       - Assert all 5 fold_<i>_result.json files exist with correct fold_index in their JSON contents.

    5. test_uses_automil_fold_count_env:
       - Set AUTOMIL_FOLD_COUNT=7 + AUTOMIL_RESULTS_DIR.
       - Call _write_fold_result_json(2, ...).
       - Assert payload["fold_count"] == 7.

    6. test_handles_missing_metrics_gracefully:
       - Call with `{"test_metrics": {}, "val_metrics": {}}` (no auc/bacc keys).
       - Assert no exception; assert payload["metrics"]["test_auc"] == 0.0 (Pitfall 5 _unwrap fallback).

    Use pytest fixtures `tmp_path` and `monkeypatch`.

    Run pytest and verify GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_per_fold_writer.py -xvs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_per_fold_writer.py` exists.
    - `grep -c "def test_" tests/test_per_fold_writer.py` returns at least 6.
    - `grep -c "_write_fold_result_json" tests/test_per_fold_writer.py` returns at least 6 (one call per test).
    - `grep -c "AUTOMIL_RESULTS_DIR" tests/test_per_fold_writer.py` returns at least 3.
    - `grep -c "monkeypatch.setenv\|monkeypatch.delenv" tests/test_per_fold_writer.py` returns at least 2.
    - `grep -c "auc_roc.*mean\|\"mean\":" tests/test_per_fold_writer.py` returns at least 1 (Pitfall 5 dict-shape covered).
    - `uv run pytest tests/test_per_fold_writer.py -x` PASSES (all 6 tests green).
    - `uv run pytest tests/ -x` passes (existing baseline).
  </acceptance_criteria>
  <done>6 unit tests cover the writer's full contract: env-gating, key mapping, CI-dict unwrap (Pitfall 5), per-fold uniqueness, fold_count env, missing-metrics fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Training subprocess (autobench) -> AUTOMIL_RESULTS_DIR/fold_<i>_result.json | Subprocess is the single writer per fold; orchestrator reads. |
| Training subprocess -> SIGTERM handler -> result.json | Handler is opt-in; without it, daemon's reconcile_budget_kill still recovers from on-disk fold files (D-122 fallback). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering | AUTOMIL_RESULTS_DIR pointed at unintended path | accept | Env var supplied by daemon (Plan 04-07); daemon trusts its own cwd. Operator running benchmarks manually with a custom AUTOMIL_RESULTS_DIR is a feature, not a threat. |
| T-04-22 | Tampering | _write_fold_result_json silently swallows exceptions | mitigate | The helper does NOT swallow exceptions in the write path — write_text raises on disk-full / permission errors and propagates to the training script (which then crashes loudly, matching existing behaviour). The only "silent" path is the AUTOMIL_RESULTS_DIR-unset noop. |
| T-04-23 | Information Disclosure | per-fold metrics on disk | accept | Same exposure as existing archive/<node>/result.json + log files. No new surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_per_fold_writer.py -x` passes all 6 tests.
- `uv run pytest tests/ -x` passes (existing baseline).
- `grep -c "def _write_fold_result_json" benchmarks/src/autobench/pipeline/clam/runner.py` returns 1.
- `grep -c "register_sigterm_flush()" benchmarks/scripts/run_experiment.py` returns at least 1.
- `grep -rE "from autobench|AUTOBENCH_" src/automil/cells/` returns zero (framework still framework-only).
</verification>

<success_criteria>
1. After each fold completes inside `runner.run_experiment()`, `fold_<i>_result.json` is written to `AUTOMIL_RESULTS_DIR` with the D-118 shape.
2. The writer is gated on `AUTOMIL_RESULTS_DIR`; runs outside the orchestrator are no-op.
3. `compute_extended_metrics` return shapes (flat float OR CI dict) are both unwrapped correctly (Pitfall 5).
4. `run_experiment.py:main()` calls `register_sigterm_flush()` before DataLoader/multiprocessing setup.
5. No autobench imports leaked into `src/automil/`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-08-SUMMARY.md` per the standard summary template.
</output>
