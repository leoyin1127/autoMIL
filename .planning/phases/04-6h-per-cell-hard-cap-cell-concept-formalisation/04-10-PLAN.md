---
phase: 04-6h-per-cell-hard-cap-cell-concept-formalisation
plan: 10
type: execute
wave: 7
depends_on: ["04-07", "04-09"]

files_modified:
  - tests/cells/test_cap_fires_with_partial_fold_recovery.py
  - tests/cells/test_cell_state_survives_daemon_kill_restart.py
  - tests/cells/test_reconcile_full.py
autonomous: true
requirements: [CAP-03, CAP-04, CAP-05]
tags: [anti-acceptance, pitfall-4, integration, kill-9, daemon-restart, load-bearing]

must_haves:
  truths:
    - "test_cap_fires_with_partial_fold_recovery: with budget=60s (NOT 21600s — paper-campaign-vs-framework rule), a synthetic 5-fold experiment that writes 3 fold files BEFORE cap fires produces archive/<node>/result.json with status='partial', partial_folds=3, composite ≠ 0, metadata.budget_killed=True; descendants are recomputed against partial composite (NOT zero); VRAM-equivalent (subprocess actually exited) within 30s of cap firing."
    - "test_cell_state_survives_daemon_kill_restart: kill -9 the daemon mid-cell, restart, assert consumed_seconds(cell) returns the elapsed time from started_at — not zero (D-111 restart-safety)."
    - "test_reconcile_full: end-to-end reconcile_budget_kill with partial folds → graph.add_executed called with the right composite; descendant cascade runs against partial composite; metadata.budget_killed=True on the graph node."
  artifacts:
    - path: tests/cells/test_cap_fires_with_partial_fold_recovery.py
      provides: "Pitfall-4 anti-acceptance gate"
      contains: "budget_seconds=60, send_signal(SIGTERM), partial_folds==3, composite>0, _reevaluate_descendants"
    - path: tests/cells/test_cell_state_survives_daemon_kill_restart.py
      provides: "CAP-05 daemon-restart test"
      contains: "kill -9, started_at, consumed_seconds"
    - path: tests/cells/test_reconcile_full.py
      provides: "End-to-end reconcile + descendant cascade test"
      contains: "reconcile_budget_kill, graph.add_executed, _reevaluate_descendants"
  key_links:
    - from: "test_cap_fires_with_partial_fold_recovery"
      to: "automil.cells.reconcile.reconcile_budget_kill"
      via: "subprocess SIGTERM -> handler -> result.json -> daemon._handle_completion -> reconcile"
      pattern: "reconcile_budget_kill|partial_folds.*== 3"
    - from: "test_cell_state_survives_daemon_kill_restart"
      to: "automil.cells.consumed_seconds"
      via: "kill -9 + restart, then assert consumed > 0 from on-disk started_at"
      pattern: "consumed_seconds.*> 0|os.kill.*SIGKILL"
---

<objective>
Land the three load-bearing integration tests for Phase 4. These are the tests Leo and the verifier inspect to decide whether Phase 4 actually works:

1. **test_cap_fires_with_partial_fold_recovery** — the Pitfall-4 anti-acceptance gate. CONTEXT.md D-126 is explicit: this test is "the goal-backward verifier for the whole phase — it must be load-bearing, not skippable." It uses `budget_seconds=60` (NOT 21600 — paper-campaign-vs-framework: 6h is Leo's autoMIL-paper default, the test must use an executable-in-3-seconds value).

2. **test_cell_state_survives_daemon_kill_restart** — CAP-05. Kill -9 the daemon, restart, prove `started_at` survived and `consumed_seconds` is computed correctly.

3. **test_reconcile_full** — exercises `reconcile_budget_kill` with a real ExperimentGraph (not a mock), proving the descendant cascade recomputes against the partial composite (Fragile Invariant #6 defence).

Purpose: Phase 4 ships only if these three tests pass. They are the single source of truth that the cap, the per-fold protocol, and the daemon restart-safety all integrate correctly. Without these, the unit tests from earlier plans give false confidence (each piece works alone, but composing them might break — these tests prove composition).

Output: 3 new test files. NO production code changes — Plans 04-01 through 04-09 are responsible for the implementations these tests exercise. If any test reveals an integration gap, the fix lands in the relevant earlier plan via revision (not here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-PATTERNS.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md

<interfaces>
From Plan 04-07 (daemon):
  ExperimentOrchestrator._tick_cells() — drives state transitions
  ExperimentOrchestrator._handle_completion(node_id, returncode) — detects cap-cancel + reconciles

From Plan 04-04 (reconcile):
  reconcile_budget_kill(node_id, archive_dir, graph, expected_fold_count) -> dict
  aggregate_folds(node_archive, expected_fold_count) -> dict

From Plan 04-05 (registry):
  get_or_create_cell(...), list_cells(), get_cell(...), consumed_seconds(cell)
  write_cell(cell, cells_dir)

From Plan 04-02 (runtime helpers):
  register_sigterm_flush() — opt-in, idempotent, exits 0 with partial result.json

From src/automil/graph.py (existing):
  ExperimentGraph(graph_path)
  graph.add_executed(node_id, composite, metrics, status, ...)
  graph._reevaluate_descendants(node_id) (or graph.reconcile)
  graph.get_node(node_id) -> dict
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/cells/test_cap_fires_with_partial_fold_recovery.py (Pitfall-4 anti-acceptance gate)</name>
  <files>tests/cells/test_cap_fires_with_partial_fold_recovery.py</files>
  <read_first>
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (D-126 — full anti-acceptance gate spec; specifics § cap-firing test design)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md (Pattern 5 fold structure + Pattern 6 aggregator algorithm)
    - tests/cells/conftest.py (Plan 04-01 — for make_cell + cells_dir)
    - tests/cells/test_runtime_helpers.py (Plan 04-02 — for the subprocess SIGTERM pattern)
    - src/automil/runtime_helpers.py (Plan 04-02 — for register_sigterm_flush API)
    - src/automil/cells/reconcile.py (Plan 04-04 — for reconcile_budget_kill signature)
    - src/automil/graph.py (read for graph.add_executed signature, _reevaluate_descendants behavior)
  </read_first>
  <action>
    Create `tests/cells/test_cap_fires_with_partial_fold_recovery.py`. The test composes the full chain end-to-end:

    Test design (per CONTEXT.md specifics § cap-firing test design):
    - Use `budget_seconds=60`, `safety_buffer_seconds=10` (NOT 21600/1800 — paper-campaign-vs-framework rule). Test must finish in seconds.
    - Use a synthetic 5-fold subprocess that:
        a. Imports register_sigterm_flush and calls it
        b. Writes 3 fold files (fold_0, fold_1, fold_2) with composites [0.80, 0.82, 0.84] sleeping briefly between to simulate fold work
        c. Sleeps 30s after fold 3 — gives parent time to send SIGTERM
    - Parent test: sends SIGTERM after the 3 folds are written, captures the subprocess exit, then invokes the framework's reconciliation pathway directly (we are not running a full daemon — the test asserts the COMPOSITION of the pieces, not the daemon-driven fully autonomous flow which is too flaky for unit tests; the unit-level test for daemon-driven cap firing is in Plan 04-07's tests/test_tick_cells.py).

    Use `subprocess.Popen` to launch the synthetic training subprocess; use `time.sleep` to wait for the 3 fold files to appear; use `proc.send_signal(signal.SIGTERM)` to trigger the cap-fire equivalent; use `proc.wait(timeout=10)` to confirm the subprocess exits cleanly (returncode 0 — Pitfall 1 verification).

    Then: call `aggregate_folds(node_archive, expected_fold_count=5)` directly to confirm the partial-result reconstruction produces partial_folds=3, status="partial", composite=mean of the 3 folds.

    Then: simulate the daemon's reconcile path by calling `reconcile_budget_kill(node_id, archive_dir, graph=mock_graph, expected_fold_count=5)` where mock_graph is a MagicMock that records add_executed calls. Assert the mock was called with:
    - composite ≈ 0.82 (mean of [0.80, 0.82, 0.84])
    - partial_folds=3 visible in the payload returned

    Then: confirm `archive/<node>/result.json` exists with `metadata.budget_killed=True`, `status="partial"`, `partial_folds=3`.

    Then (descendant cascade): construct a real ExperimentGraph (in the test's tmp_path) with a parent node having composite=0.50 and a child node = our cap-killed node with composite=0.82. After reconcile, call graph._reevaluate_descendants(node_id) (or whatever the existing function is named — read src/automil/graph.py). Assert that the descendant logic operates on the partial composite (0.82) — i.e., a grandchild node with composite=0.85 would be marked "keep" (improving on 0.82), not unconditionally discarded against zero. This is Fragile Invariant #6.

    Test layout:

      """Pitfall-4 anti-acceptance gate (CAP-03, CAP-04 / D-126).

      Goal-backward verifier for Phase 4: cap-firing must produce a usable
      partial composite, NOT corrupt results.tsv. budget_seconds=60 here is
      a TEST-ONLY value chosen to make the test executable in seconds —
      Leo's autoMIL-paper campaign uses 21600 (6h), but the test deliberately
      uses 60s because the framework property is the *mechanism*, not the
      *value* (paper_campaign_vs_framework rule).
      """
      from __future__ import annotations
      import json, os, signal, subprocess, sys, textwrap, time
      from pathlib import Path
      from unittest.mock import MagicMock
      import pytest

      pytestmark = pytest.mark.skipif(sys.platform == "win32", reason="SIGTERM unsupported on Windows")

      def test_cap_fires_with_partial_fold_recovery(tmp_path: Path):
          # 1. Set up synthetic node archive
          node_id = "node_test_cap_fire"
          archive_dir = tmp_path / "archive"
          node_archive = archive_dir / node_id
          node_archive.mkdir(parents=True)

          # 2. Write a synthetic training script that registers the SIGTERM handler,
          #    writes 3 fold files (0, 1, 2), then sleeps. Parent will SIGTERM after
          #    the 3 files exist.
          script = textwrap.dedent('''
              import json, os, time, sys
              from pathlib import Path
              from automil.runtime_helpers import register_sigterm_flush
              register_sigterm_flush()
              archive = Path(os.environ["AUTOMIL_RESULTS_DIR"])
              for i, comp in enumerate([0.80, 0.82, 0.84]):
                  payload = {
                      "fold_index": i, "fold_count": 5, "status": "completed",
                      "metrics": {"val_auc": comp, "val_bacc": comp,
                                  "test_auc": comp, "test_bacc": comp},
                      "composite": comp, "elapsed_seconds": 1, "peak_vram_mb": 1000,
                  }
                  (archive / f"fold_{i}_result.json").write_text(json.dumps(payload))
                  sys.stdout.flush()
                  time.sleep(0.05)
              # Signal readiness — parent waits for marker
              (archive / "_ready").write_text("ready")
              # Now sleep to give parent time to SIGTERM us
              time.sleep(30)
          ''')

          env = os.environ.copy()
          env["AUTOMIL_RESULTS_DIR"] = str(node_archive)
          env["AUTOMIL_FOLD_COUNT"] = "5"

          proc = subprocess.Popen(
              [sys.executable, "-c", script], env=env,
              stdout=subprocess.PIPE, stderr=subprocess.PIPE,
          )

          # 3. Wait for 3 fold files + ready marker (cap_fire trigger)
          deadline = time.time() + 30
          while time.time() < deadline:
              if (node_archive / "_ready").exists():
                  break
              if proc.poll() is not None:
                  stdout, stderr = proc.communicate()
                  pytest.fail(f"Subprocess died before writing folds. stderr={stderr.decode(errors='replace')}")
              time.sleep(0.1)

          assert (node_archive / "fold_0_result.json").exists()
          assert (node_archive / "fold_1_result.json").exists()
          assert (node_archive / "fold_2_result.json").exists()
          assert not (node_archive / "fold_3_result.json").exists()
          assert not (node_archive / "fold_4_result.json").exists()

          # 4. Send SIGTERM (cap fire equivalent) — handler must flush partial result.json
          cap_fire_time = time.time()
          proc.send_signal(signal.SIGTERM)
          stdout, stderr = proc.communicate(timeout=10)

          # 5. VRAM-equivalent assertion: subprocess actually exited within 30s
          elapsed_to_exit = time.time() - cap_fire_time
          assert elapsed_to_exit < 30, f"Subprocess did not exit in 30s after SIGTERM (elapsed={elapsed_to_exit:.1f}s)"
          assert proc.returncode == 0, (
              f"SIGTERM should produce returncode 0 (clean exit via handler). "
              f"got {proc.returncode}; stderr={stderr.decode(errors='replace')}"
          )

          # 6. Assert SIGTERM handler wrote result.json with partial status
          result_path = node_archive / "result.json"
          assert result_path.exists(), "SIGTERM handler did not write result.json"
          result = json.loads(result_path.read_text())
          assert result["status"] == "partial", f"expected partial, got {result['status']}"
          assert result["partial_folds"] == 3
          assert result["expected_folds"] == 5
          assert result["composite"] > 0.0, "composite must NOT be zero (Fragile Invariant #6 defence)"
          assert abs(result["composite"] - 0.82) < 0.01, "composite must be mean of [0.80, 0.82, 0.84]"

          # 7. Now simulate the daemon's reconcile pathway end-to-end via reconcile_budget_kill.
          #    This is the framework path the daemon takes when it observes cap-cancel completion.
          #    We use a MagicMock for graph so we can assert add_executed was called correctly,
          #    plus a separate real-graph step in test_reconcile_full.
          from automil.cells.reconcile import reconcile_budget_kill
          mock_graph = MagicMock()
          payload = reconcile_budget_kill(
              node_id=node_id,
              archive_dir=archive_dir,
              graph=mock_graph,
              expected_fold_count=5,
          )

          # The stub from Plan 04-04 writes result.json + budget_killed flag and returns the payload.
          # (Daemon calls graph.add_executed itself in Plan 04-07; we don't assert on mock_graph here.)
          assert payload["status"] == "partial"
          assert payload["partial_folds"] == 3
          assert payload["composite"] > 0.0
          assert payload.get("metadata", {}).get("budget_killed") is True

          # 8. Reload result.json from disk — reconcile_budget_kill should have rewritten it
          result_after_reconcile = json.loads(result_path.read_text())
          assert result_after_reconcile.get("metadata", {}).get("budget_killed") is True

          # 9. Fragile Invariant #6 defence: real-graph descendant cascade against the
          #    PARTIAL composite (0.82), NOT zero. This step is what makes Pitfall-4
          #    single-file load-bearing — without it a reviewer running only this
          #    test would not see the cascade verified. (Plan 04-10 Task 3 also
          #    exercises this with a more elaborate scenario, but Pitfall-4 owns
          #    the smoke-level proof.)
          #
          #    PINNED API (verified by reading src/automil/graph.py at planning time):
          #      ExperimentGraph(path: str|Path)
          #      add_executed(parent_id, description, techniques, metrics,
          #                   status="discard", ...) -> nid (generated)
          #      get_node(nid) -> dict | None
          #      _reevaluate_descendants(root_id) -> None
          #    Cascade rule (graph.py:267):
          #      keep = (c_auc >= p_auc AND c_bacc >= p_bacc AND c_comp > p_comp)
          #    So descendant must beat parent on AUC AND BACC AND composite.
          from automil.graph import ExperimentGraph
          eg = ExperimentGraph(tmp_path / "descendant_graph.json")
          # Parent node with weak composite/AUC/BACC (0.50)
          parent_nid = eg.add_executed(
              parent_id=None, description="parent", techniques=[],
              metrics={"composite": 0.50, "test_auc": 0.50, "test_bacc": 0.50},
              status="keep",
          )
          # Cap-killed node registered with the PARTIAL composite (0.82)
          capkill_nid = eg.add_executed(
              parent_id=parent_nid, description="cap-killed (partial)", techniques=[],
              metrics={"composite": 0.82, "test_auc": 0.82, "test_bacc": 0.82},
              status="keep",
          )
          # Descendant that beats partial composite/AUC/BACC (0.85) — must remain
          # "keep" after cascade. If the cascade incorrectly evaluated against
          # zero (Fragile Invariant #6 break), it would still be "keep" trivially,
          # so we ALSO add a descendant that beats 0 but loses to 0.82 — that one
          # must FLIP from "keep" to "discard" if the cascade ran correctly.
          better_nid = eg.add_executed(
              parent_id=capkill_nid, description="better than partial", techniques=[],
              metrics={"composite": 0.85, "test_auc": 0.85, "test_bacc": 0.85},
              status="keep",
          )
          worse_nid = eg.add_executed(
              parent_id=capkill_nid, description="worse than partial, beats zero",
              techniques=[],
              metrics={"composite": 0.70, "test_auc": 0.70, "test_bacc": 0.70},
              status="keep",
          )
          eg._reevaluate_descendants(capkill_nid)
          assert eg.get_node(better_nid)["status"] == "keep", (
              "Descendant beating partial composite 0.82 should remain 'keep'."
          )
          assert eg.get_node(worse_nid)["status"] == "discard", (
              "Descendant losing to partial composite 0.82 should flip to 'discard'. "
              "If it stayed 'keep', the cascade ran against zero — Fragile Invariant #6 broken."
          )

    NOTE: this test relies on the framework already being implemented end-to-end (Plans 04-01..04-04 must be GREEN). If Plans 04-04's reconcile_budget_kill stub does NOT include the result.json metadata.budget_killed flag, this test will fail — which is the correct signal that the stub needs to be promoted to a full implementation in 04-04 (the stub does include `payload.setdefault("metadata", {})["budget_killed"] = True` per the Plan 04-04 action, so this should pass). Step 9's graph API is pinned at planning time against `src/automil/graph.py:35,112,241,267` — if the file changes between planning and execution, implementer must verify and adapt.
  </action>
  <verify>
    <automated>uv run pytest tests/cells/test_cap_fires_with_partial_fold_recovery.py -xvs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/cells/test_cap_fires_with_partial_fold_recovery.py` exists.
    - `grep -c "def test_cap_fires_with_partial_fold_recovery" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns 1.
    - `grep -c "subprocess.Popen\|subprocess\\.Popen" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns 1.
    - `grep -c "send_signal(signal.SIGTERM)" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns 1.
    - `grep -c "proc.returncode == 0" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns 1 (clean exit asserted).
    - `grep -c '\"status\".*\"partial\"\\|status.*== \"partial\"' tests/cells/test_cap_fires_with_partial_fold_recovery.py | tr -d '\\n'` is at least 1.
    - `grep -c 'partial_folds.*== 3\\|partial_folds.*: 3' tests/cells/test_cap_fires_with_partial_fold_recovery.py | tr -d '\\n'` is at least 1.
    - `grep -c 'composite.*> 0' tests/cells/test_cap_fires_with_partial_fold_recovery.py | tr -d '\\n'` is at least 1 (composite-not-zero asserted — Fragile Invariant #6).
    - `grep -c "budget_killed" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns at least 2 (metadata + reconcile assertions).
    - `grep -cE "21600\b" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns 0 — test MUST use 60s budget, NOT 21600 (paper-campaign-vs-framework rule).
    - `grep -c "register_sigterm_flush" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns at least 1 (in the synthetic script).
    - `grep -c "reconcile_budget_kill" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns at least 1.
    - `grep -c "_reevaluate_descendants\|reevaluate_descendants" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns at least 1 (Fragile Invariant #6 cascade asserted in this load-bearing file, not just delegated to test_reconcile_full).
    - `grep -c "ExperimentGraph" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns at least 1 (real graph, not just MagicMock).
    - `grep -c "child_better.*== \"keep\"\|child_after\\[.status.\\].*==.*.keep." tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns at least 1 (descendant beats partial composite).
    - `uv run pytest tests/cells/test_cap_fires_with_partial_fold_recovery.py -x` PASSES.
  </acceptance_criteria>
  <done>End-to-end SIGTERM -> handler -> partial result.json -> reconcile_budget_kill chain works; partial composite is non-zero; metadata.budget_killed propagates through; subprocess truly exits within 30s of SIGTERM (VRAM-equivalent assertion).</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/cells/test_cell_state_survives_daemon_kill_restart.py (CAP-05)</name>
  <files>tests/cells/test_cell_state_survives_daemon_kill_restart.py</files>
  <read_first>
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (specifics § daemon-restart test design + D-111)
    - tests/cells/conftest.py (Plan 04-01)
    - src/automil/cells/state.py + registry.py (Plans 04-01, 04-05)
  </read_first>
  <action>
    Create `tests/cells/test_cell_state_survives_daemon_kill_restart.py`. The test does NOT actually need to spawn a daemon process — the property under test is "started_at survives ANY restart" because the value is on disk. We simulate the daemon kill by:
    1. Writing a Cell to disk via `write_cell` with `started_at = time.time() - 14400` (4h ago).
    2. Calling `read_cell(...)` (or `get_cell(...)`) to simulate "fresh process" reads the file.
    3. Asserting `consumed_seconds(reloaded_cell) ≈ 14400` (NOT 0).

    But to truly assert the daemon-restart invariant, also include a SECOND test that uses subprocess.run to spawn a fresh Python process (clean module state, no in-memory cache) and have IT read the cell file. This proves the persistence is process-independent.

    Tests:

    1. test_started_at_persists_across_in_process_reload:
       - cells_dir from fixture
       - fake_now = 1_000_000_000.0
       - 4h_ago = fake_now - 14400
       - Write Cell with started_at=4h_ago using write_cell.
       - Re-read via read_cell. Assert reloaded.started_at == 4h_ago (exact, not "approximately").

    2. test_consumed_seconds_computed_from_persisted_started_at:
       - Use real time.time() — write a cell with started_at = time.time() - 14400.
       - Re-read; assert consumed_seconds(reloaded) is in [14395, 14410] (allow ±10s for test execution drift).
       - Crucially: assert NOT 0.

    3. test_started_at_persists_across_subprocess_restart:
       - Use a real fake_automil_dir setup (monkeypatch _find_automil_dir).
       - Use get_or_create_cell to open a cell (started_at recorded).
       - Read started_at, dataset, encoder, parent_id values from the on-disk file.
       - Spawn a subprocess that imports automil.cells and calls get_cell(cell_id), then prints `cell.started_at` and `consumed_seconds(cell)` to stdout.
       - The subprocess must access the SAME cells/<id>.json — pass the path via env var (e.g. AUTOMIL_TEST_CELLS_DIR), and have the subprocess re-implement get_cell directly: `data = json.loads(Path(os.environ["AUTOMIL_TEST_CELLS_DIR"]) / f"{cell_id}.json").read_text(); print(data["started_at"])` — keeping the subprocess scope simple.
       - Assert subprocess stdout contains the same started_at and that subprocess-computed consumed_seconds is > 0.

    4. test_status_transitions_persist_across_reload:
       - Write a Cell with status=ACTIVE.
       - Reload via read_cell, then write_cell(replace(cell, status=CellStatus.REFUSING_NEW), cells_dir).
       - Reload again; assert status == CellStatus.REFUSING_NEW.
       - Assert started_at is unchanged (transition didn't reset the wall clock).

    5. test_zero_accumulator_pattern_in_state_module:
       - This is a STATIC asserter — open `src/automil/cells/state.py` and assert that `+= ` does not appear in the source for `consumed_seconds`. This catches future regressions where someone re-introduces an accumulator.
       - Specifically: `text = Path("src/automil/cells/state.py").read_text(); assert "+= " not in text` (or be more targeted: assert `consumed_seconds` body has no augmented assignments).

    Run pytest and confirm GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/cells/test_cell_state_survives_daemon_kill_restart.py -xvs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/cells/test_cell_state_survives_daemon_kill_restart.py` exists.
    - `grep -c "def test_" tests/cells/test_cell_state_survives_daemon_kill_restart.py` returns at least 5.
    - `grep -c "subprocess" tests/cells/test_cell_state_survives_daemon_kill_restart.py` returns at least 1 (Test 3 uses subprocess).
    - `grep -c "consumed_seconds" tests/cells/test_cell_state_survives_daemon_kill_restart.py` returns at least 3.
    - `grep -c "started_at" tests/cells/test_cell_state_survives_daemon_kill_restart.py` returns at least 5.
    - `grep -c "14400\\|4 \\* 3600\\|4\\*3600" tests/cells/test_cell_state_survives_daemon_kill_restart.py` returns at least 1 (4h-ago marker).
    - `grep -c "+= " tests/cells/test_cell_state_survives_daemon_kill_restart.py` returns at least 1 (Test 5 source assertion looking for the anti-pattern in state.py).
    - `uv run pytest tests/cells/test_cell_state_survives_daemon_kill_restart.py -x` PASSES (all 5 tests green).
  </acceptance_criteria>
  <done>started_at persists across in-process reload, subprocess restart, status transitions; consumed_seconds is computed not accumulated (verified statically); zero-accumulator regression guard in place.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests/cells/test_reconcile_full.py (end-to-end reconcile + descendant cascade)</name>
  <files>tests/cells/test_reconcile_full.py</files>
  <read_first>
    - src/automil/graph.py (read for ExperimentGraph constructor + add_executed + _reevaluate_descendants exact API; the cascade is the key thing this test exercises)
    - src/automil/cells/reconcile.py (Plan 04-04)
    - tests/test_graph.py (read for ExperimentGraph test setup patterns — graph_path, populate, etc.)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (D-123, D-124, also Pitfall 4 anti-acceptance specifics)
  </read_first>
  <action>
    Create `tests/cells/test_reconcile_full.py` exercising the full reconcile_budget_kill -> graph -> descendant cascade chain. Unlike Task 1's test (which uses MagicMock for graph), this test uses a REAL ExperimentGraph backed by tmp_path / "graph.json", proving that:

    1. partial_folds >= 1 → graph node ends up with composite = mean of fold composites, status="executed" (or whatever the keep/discard logic decides), metadata.budget_killed=True.
    2. partial_folds == 0 → graph node ends up with status="crash", metadata.budget_killed=True.
    3. Descendant cascade operates on the partial composite (Fragile Invariant #6).

    Tests:

    1. test_reconcile_budget_kill_writes_partial_result_json:
       - Set up tmp_path/archive/<node_id>/ with 3 fold files (composites [0.80, 0.82, 0.84]).
       - Call reconcile_budget_kill(node_id, archive_dir, graph=MagicMock(), expected_fold_count=5).
       - Assert archive/<node_id>/result.json exists; load it and assert status="partial", composite≈0.82, metadata.budget_killed=True.

    2. test_reconcile_budget_kill_zero_folds_writes_crashed_result_json:
       - archive/<node_id>/ exists but is empty.
       - Call reconcile_budget_kill.
       - Assert result.json status="crashed", composite=0.0, metadata.budget_killed=True.

    3. test_descendant_cascade_against_partial_composite_keeps_better_descendant:
       - Construct an ExperimentGraph with: parent (composite=0.50), the cap-killed node, and a hypothetical descendant.
       - Reconcile the cap-killed node to executed with composite=0.82.
       - Now ALSO add a descendant node with composite=0.85 (better than 0.82).
       - Trigger graph._reevaluate_descendants (or equivalent existing API).
       - Assert: the descendant is marked "keep" (improving on partial 0.82, not unconditionally discarded against 0.0).
       - The exact assertion depends on the existing graph API — read src/automil/graph.py to confirm whether the cascade is invoked automatically by add_executed or manually. Adapt accordingly.

    4. test_descendant_cascade_against_partial_composite_discards_worse_descendant:
       - Same as 3 but descendant composite=0.70 (worse than 0.82).
       - Assert descendant is marked "discard" (Pareto dominance against partial 0.82, not against 0).

    5. test_reconcile_logs_summary_at_info_level:
       - Use caplog at INFO.
       - Run reconcile_budget_kill with 2 folds.
       - Assert a log record contains "partial" and "2/" (partial_folds=2 marker).

    NOTE: Tests 3 and 4 require the implementer to read src/automil/graph.py FIRST and adapt the test to whatever the existing keep/discard API looks like. If the cascade is internal to add_executed (auto-triggered), test it indirectly by reading the resulting graph.json. If it's a separate call, invoke it explicitly.

    Run pytest and confirm GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/cells/test_reconcile_full.py -xvs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/cells/test_reconcile_full.py` exists.
    - `grep -c "def test_" tests/cells/test_reconcile_full.py` returns at least 5.
    - `grep -c "reconcile_budget_kill" tests/cells/test_reconcile_full.py` returns at least 5.
    - `grep -c "ExperimentGraph" tests/cells/test_reconcile_full.py` returns at least 1 (real graph used in tests 3+4).
    - `grep -c "keep\\|discard" tests/cells/test_reconcile_full.py` returns at least 2 (descendant cascade verified).
    - `grep -c "budget_killed" tests/cells/test_reconcile_full.py` returns at least 2.
    - `grep -c "0.82\\|0.85\\|0.70" tests/cells/test_reconcile_full.py` returns at least 3 (partial composite values used in cascade assertions).
    - `grep -c "caplog" tests/cells/test_reconcile_full.py` returns at least 1 (Test 5).
    - `uv run pytest tests/cells/test_reconcile_full.py -x` PASSES.
  </acceptance_criteria>
  <done>End-to-end reconcile + descendant cascade chain proven against real ExperimentGraph; partial composite flows through Pareto keep/discard correctly; result.json metadata.budget_killed and status="crashed"|"partial" both verified.</done>
</task>

<task type="auto">
  <name>Task 4: Run the full Phase 4 test suite + verify zero autobench leakage</name>
  <files>tests/cells/</files>
  <read_first>
    - All Plan 04-01..04-09 SUMMARY files (if they exist) for any known issues
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (hard floors + D-126 acceptance gate)
  </read_first>
  <action>
    No new files created in this task. Run the full Phase 4 acceptance gate end-to-end and verify all 9 conditions from D-126 are met:

    1. Run `uv run pytest tests/cells/ -v` — all cells tests must pass.
    2. Run `uv run pytest tests/test_submit_cell_refusal.py tests/test_tick_cells.py tests/test_per_fold_writer.py tests/test_cli_cell.py -v` — all Phase-4-adjacent integration tests must pass.
    3. Run `uv run pytest tests/ -v` — full suite must pass (existing 558+9 baseline preserved).
    4. Run `grep -rE "autobench|AUTOBENCH_|benchmarks/" src/automil/cells/ src/automil/cli/cell.py src/automil/runtime_helpers.py` — must return ZERO lines.
    5. Run `grep -c "+= " src/automil/cells/state.py` — must return 0 (no consumed_seconds accumulator regression).
    6. Run `uv run automil cell --help` — must show status + list subcommands.
    7. Run `uv run automil cell list` — should print "(no cells)" or list any test residual cells without crashing.

    If ANY of these fail, the failure must be traced back to the responsible Plan (04-01..04-09) and that plan must be revised — Plan 04-10's role here is verification, NOT bug-fixing in earlier plans (that goes through the GSD revision flow).

    Document the results in the eventual `04-10-SUMMARY.md` (created at plan completion):
    - Total tests run, passed, skipped
    - Time to run full suite
    - Any Phase 4 hard-floor that did NOT meet acceptance, with link to the responsible plan

    NO new code, NO new tests in this task — pure verification.
  </action>
  <verify>
    <automated>uv run pytest tests/cells/ tests/test_submit_cell_refusal.py tests/test_tick_cells.py tests/test_per_fold_writer.py tests/test_cli_cell.py -v 2>&1 | tail -10 && echo "---" && uv run pytest tests/ -q 2>&1 | tail -5 && echo "---" && grep -rE "autobench|AUTOBENCH_|benchmarks/" src/automil/cells/ src/automil/cli/cell.py src/automil/runtime_helpers.py | wc -l && echo "(must be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/cells/ -v` exits 0 (all Phase 4 tests in tests/cells/ pass).
    - `uv run pytest tests/test_submit_cell_refusal.py tests/test_tick_cells.py tests/test_per_fold_writer.py tests/test_cli_cell.py -v` exits 0.
    - `uv run pytest tests/ -v` exits 0 (existing 558+9 baseline preserved, plus all new Phase 4 tests).
    - `grep -rE "autobench|AUTOBENCH_|benchmarks/" src/automil/cells/ src/automil/cli/cell.py src/automil/runtime_helpers.py | wc -l` returns 0.
    - `grep -c "+= " src/automil/cells/state.py` returns 0.
    - `uv run automil cell --help` lists "status" and "list" subcommands.
    - `uv run automil cell list` runs without crashing (output may be "(no cells)").
  </acceptance_criteria>
  <done>Full Phase 4 acceptance gate passes; zero framework-leakage; existing baseline preserved; CLI is operator-usable end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> tmp_path filesystem | Tests own their tmp_path; no production data touched. |
| Subprocess test child -> SIGTERM | Test parent sends SIGTERM; child's handler (production code from Plan 04-02) writes result.json. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Denial of Service | Subprocess fails to exit within 30s of SIGTERM | mitigate | Test asserts `proc.returncode == 0` and `elapsed_to_exit < 30`; if violated, the test fails loudly — which is the correct signal that Plan 04-02's handler is broken. VRAM-release proxy. |
| T-04-28 | Tampering | Test uses 21600 (paper-campaign value) instead of 60 (test value) | mitigate | Acceptance grep `grep -cE "21600\b" tests/cells/test_cap_fires_with_partial_fold_recovery.py` returns 0 — test must use the small test budget per paper_campaign_vs_framework rule. |
| T-04-29 | Tampering | Future regression re-introduces consumed_seconds accumulator | mitigate | test_zero_accumulator_pattern_in_state_module asserts `+= ` is absent from state.py — static guard against the sandbagging anti-pattern. |
</threat_model>

<verification>
- `uv run pytest tests/cells/test_cap_fires_with_partial_fold_recovery.py -x` passes — Pitfall-4 anti-acceptance gate.
- `uv run pytest tests/cells/test_cell_state_survives_daemon_kill_restart.py -x` passes — CAP-05 restart-safety.
- `uv run pytest tests/cells/test_reconcile_full.py -x` passes — descendant cascade against partial composite.
- `uv run pytest tests/ -x` passes (full suite green, existing baseline preserved).
- `grep -rE "autobench|AUTOBENCH_|benchmarks/" src/automil/cells/ src/automil/cli/cell.py src/automil/runtime_helpers.py` returns zero.
</verification>

<success_criteria>
1. Pitfall-4 anti-acceptance test passes end-to-end: SIGTERM -> handler -> partial result.json -> reconcile -> graph node executed with budget_killed=True.
2. Daemon-restart-survival test passes: started_at persists across reload + subprocess; consumed_seconds correctly recomputed.
3. Reconcile + descendant cascade test passes: partial composite flows through Pareto keep/discard logic correctly (Fragile Invariant #6 defended).
4. All 9 conditions from CONTEXT.md D-126 are demonstrably met.
5. Zero autobench leakage in framework files.
6. Existing 558+9 baseline preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-10-SUMMARY.md` per the standard summary template. Include the full pytest output of `uv run pytest tests/ -q | tail -10` as evidence the Phase 4 acceptance gate passed.
</output>
