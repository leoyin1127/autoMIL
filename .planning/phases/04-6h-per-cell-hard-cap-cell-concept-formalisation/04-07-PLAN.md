---
phase: 04-6h-per-cell-hard-cap-cell-concept-formalisation
plan: 07
type: execute
wave: 5
depends_on: ["04-03", "04-04", "04-05"]

files_modified:
  - src/automil/backends/_orchestrator_daemon.py
  - tests/test_tick_cells.py
autonomous: true
requirements: [CAP-02, CAP-04]
tags: [daemon, tick-cells, state-machine, cancel, reconcile, integration]

must_haves:
  truths:
    - "_orchestrator_daemon.tick() inserts a new step _tick_cells() between _check_running() and the pending-scheduling block (D-114 step 1.5)."
    - "_tick_cells() iterates list_cells(), calls next_status(cell, time.time(), len(_running_in_cell(cell.cell_id))), and persists transitions via dataclasses.replace + write_cell."
    - "On the ACTIVE -> REFUSING_NEW transition, no cancel is fired (only persistence)."
    - "On the REFUSING_NEW -> TERMINATING transition, the daemon writes metadata.cancel_reason='cap' to running/<node>.json BEFORE calling backend.cancel(handle, signal=SIGTERM) for every in-cell running experiment."
    - "On the TERMINATING -> FINALIZED transition, no extra action (cancel already issued); just persist."
    - "_running_in_cell(cell_id) returns JobHandles whose spec.metadata.cell_id == cell_id, drawn from self.running dict."
    - "_handle_completion (or its equivalent) detects metadata.cancel_reason == 'cap' on cancelled nodes and calls reconcile_budget_kill(node_id, archive_dir, graph, expected_fold_count); if reconcile returns partial_folds >= 1, the graph node is upgraded to status='executed' with metadata.budget_killed=True; if 0, status='crash' with metadata.budget_killed=True."
    - "AUTOMIL_FOLD_COUNT env var is injected into the experiment subprocess via _build_subprocess_env (so the SIGTERM handler can read it from inside the training script)."
  artifacts:
    - path: src/automil/backends/_orchestrator_daemon.py
      provides: "tick() extension + _tick_cells + _running_in_cell + reconcile_budget_kill integration + AUTOMIL_FOLD_COUNT env injection"
      contains: "_tick_cells, _running_in_cell, AUTOMIL_FOLD_COUNT, reconcile_budget_kill, metadata.cancel_reason"
    - path: tests/test_tick_cells.py
      provides: "Daemon-level integration tests for _tick_cells behaviour"
      contains: "test_tick_cells_active_to_refusing_new, test_tick_cells_terminating_fires_cancel_with_cap_reason, test_tick_cells_finalized_when_running_empty, test_running_in_cell_filters_by_cell_id, test_handle_completion_calls_reconcile_budget_kill_on_cap_cancel, test_automil_fold_count_injected_into_env"
  key_links:
    - from: "_orchestrator_daemon._tick_cells"
      to: "automil.cells.next_status"
      via: "pure function call inside daemon tick"
      pattern: "next_status\\(cell, now, "
    - from: "_orchestrator_daemon (TERMINATING transition)"
      to: "self.backend.cancel(handle, signal=SIGTERM)"
      via: "after writing metadata.cancel_reason='cap' to running/<node>.json"
      pattern: "self\\.backend\\.cancel\\(handle"
    - from: "_orchestrator_daemon._handle_completion"
      to: "automil.cells.reconcile_budget_kill"
      via: "when cancelled spec has metadata.cancel_reason=='cap'"
      pattern: "reconcile_budget_kill"
---

<objective>
Wire the cap state machine into the orchestrator daemon. Add `_tick_cells()` as a new step in `tick()`. Add `_running_in_cell()` helper. On `TERMINATING` transitions, annotate `running/<node>.json` with `metadata.cancel_reason='cap'` before calling `backend.cancel(handle, signal=SIGTERM)`. On node completion, if cancel_reason was 'cap', invoke `reconcile_budget_kill(node_id, ...)` and update the graph (executed if partial_folds>=1, crash otherwise). Inject `AUTOMIL_FOLD_COUNT` env var into experiment subprocesses.

Purpose: D-114 + D-123 + D-124. This is where the cap layer fires. Without this plan, cells can transition status on disk but the daemon never reads them, never cancels, and never reconciles. Plan 04-09 will add the `automil cell` CLI to inspect this state. Plan 04-11 is the end-to-end Pitfall-4 test that exercises the full chain.

Output: 1 modified large file (the daemon), 1 new test file. This is a focused, surgical edit — the daemon is ~750 lines and the existing 48-test suite is the safety net. NO restructuring of the daemon; only additive insertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-PATTERNS.md
@.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md

<interfaces>
From src/automil/backends/_orchestrator_daemon.py — current tick() shape (line approx 938-961):

  def tick(self):
      self._reload_orchestrator_config()    # step 0
      self._check_running()                 # step 1
      # NEW INSERT: self._tick_cells()      # step 1.5  (this plan adds it)
      if not self.draining:                 # step 2
          pending = self._get_pending()
          for spec in pending:
              ...
      self._save_state()                    # step 3

self.running is a dict[str, RunningExperiment] keyed by node_id.
RunningExperiment has .handle (JobHandle) and .spec (dict). spec is loaded from running/<node>.json at submit time and contains the metadata dict (with backend, runtime, cell_id from Plan 04-06).

self.running_dir is the path to running/.
self.archive_dir is the path to archive/.
self.backend is the Backend instance (LocalBackend or MockSLURMBackend in tests).
self.graph is an ExperimentGraph (graph.add_executed, graph.mark_failed exist).
_handle_completion(node_id, returncode) is the existing handler invoked when a running experiment exits.

From Plan 04-04 (just landed):
  reconcile_budget_kill(node_id, archive_dir, graph, expected_fold_count) -> dict (writes result.json + tags budget_killed; the stub does NOT mutate graph yet — that part lands HERE in Plan 04-07)

From Plan 04-03 (just landed): next_status(cell, now_epoch, running_count) -> CellStatus
From Plan 04-05 (just landed): list_cells() -> list[Cell]; get_cell(cell_id) -> Cell|None

graph.add_executed shape: graph.add_executed(node_id, composite, metrics, status="keep" or "discard", ...) — confirm exact signature by reading src/automil/graph.py.
graph.mark_failed shape: graph.mark_failed(node_id, status="crash", ...) — confirm via direct read.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _tick_cells + _running_in_cell + extend tick() in _orchestrator_daemon.py</name>
  <files>src/automil/backends/_orchestrator_daemon.py</files>
  <read_first>
    - src/automil/backends/_orchestrator_daemon.py (entire file — read the tick() method around line 938-961, _check_running, _handle_completion, _kill_experiment, RunningExperiment dataclass, _build_subprocess_env if present)
    - src/automil/cells/__init__.py (Plan 04-05 — for list_cells, next_status, write_cell, CellStatus, Cell exports)
    - src/automil/graph.py (read graph.add_executed and graph.mark_failed exact signatures)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-CONTEXT.md (D-114, D-123, D-124, also D-115 cancel signal)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-PATTERNS.md (lines 700-784 — _tick_cells extension section)
    - .planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-RESEARCH.md (Pitfall 4 — must write cancel_reason BEFORE backend.cancel)
  </read_first>
  <action>
    Make 4 surgical insertions to `src/automil/backends/_orchestrator_daemon.py`:

    1) Add `_tick_cells()` method on the ExperimentOrchestrator class. Place it near _check_running (proximity for review). Implementation:

       def _tick_cells(self) -> None:
           """Advance cap state machine for all cells (CAP-02 / D-114).

           Idempotent: re-running on an already-transitioned cell is a no-op
           because next_status returns the same value when consumed/running counts
           are stable. TERMINATING fires backend.cancel(SIGTERM) on all running
           in-cell experiments AFTER annotating their running/<node>.json with
           metadata.cancel_reason='cap' so reconcile_budget_kill can distinguish
           cap kills from operator cancels (Pitfall 4).

           Process-group kill is the backend's responsibility (D-115).
           """
           import signal as _sig
           from dataclasses import replace
           from automil.cells import list_cells, next_status, write_cell, CellStatus
           from automil.cells.registry import _cells_dir

           now = time.time()
           cells_dir = _cells_dir()
           for cell in list_cells():
               running = self._running_in_cell(cell.cell_id)
               new_status = next_status(cell, now, len(running))
               if new_status == cell.status:
                   continue
               if new_status == CellStatus.TERMINATING:
                   for handle in running:
                       running_spec_path = self.running_dir / f"{handle.node_id}.json"
                       if running_spec_path.exists():
                           try:
                               spec_data = json.loads(running_spec_path.read_text())
                               spec_data.setdefault("metadata", {})["cancel_reason"] = "cap"
                               running_spec_path.write_text(json.dumps(spec_data, indent=2))
                           except (json.JSONDecodeError, OSError) as exc:
                               logger.warning(
                                   "Could not annotate cancel_reason for %s: %s",
                                   handle.node_id, exc,
                               )
                       try:
                           self.backend.cancel(handle, signal=_sig.SIGTERM)
                       except Exception as exc:
                           logger.warning("backend.cancel failed for %s: %s", handle.node_id, exc)
               write_cell(replace(cell, status=new_status), cells_dir)
               logger.info(
                   "_tick_cells: %s transitioned %s -> %s (consumed=%.0fs/%ds, running=%d)",
                   cell.cell_id[:8], cell.status.value, new_status.value,
                   now - cell.started_at, cell.budget_seconds, len(running),
               )

    2) Add `_running_in_cell(cell_id)` helper:

       def _running_in_cell(self, cell_id: str) -> list:
           """Return JobHandles for in-self.running experiments tagged with this cell_id.

           cell_id matching uses spec.metadata.cell_id (set by submit, Plan 04-06).
           Legacy nodes without a cell_id never match — they are immune to cap
           enforcement (D-117 backward compat).
           """
           result = []
           for exp in self.running.values():
               spec_meta = (getattr(exp, "spec", {}) or {}).get("metadata", {}) or {}
               if spec_meta.get("cell_id") == cell_id:
                   result.append(exp.handle)
           return result

       NOTE: confirm by reading the daemon's RunningExperiment dataclass that .spec exists; if .spec is not stored on RunningExperiment but rather is read from disk, fall back to reading running/<node>.json. The pattern map at lines 760-765 shows the in-memory variant.

    3) Insert the tick() call. Find the existing tick() method (around line 938-961). Add `self._tick_cells()` between `self._check_running()` and the pending-scheduling block. Comment the line: `# Phase 4 step 1.5: cap state machine (D-114).`

    4) Wire reconcile_budget_kill into _handle_completion. Find `_handle_completion(self, node_id, returncode)` (or whatever the existing completion handler is named — the daemon's main path that runs when an experiment exits). After the existing result-collection / archive-move logic but BEFORE the descendant-cascade trigger, insert:

       # Phase 4: detect cap-driven cancel and reconcile to executed (CAP-04 / D-123, D-124)
       running_spec_path = self.running_dir / f"{node_id}.json"
       cap_killed = False
       if running_spec_path.exists():
           try:
               _spec = json.loads(running_spec_path.read_text())
               cap_killed = _spec.get("metadata", {}).get("cancel_reason") == "cap"
           except (json.JSONDecodeError, OSError):
               pass
       # ALSO check archive in case running/ already moved (race window):
       if not cap_killed:
           archive_spec = self.archive_dir / node_id / "spec.json"
           if archive_spec.exists():
               try:
                   _spec = json.loads(archive_spec.read_text())
                   cap_killed = _spec.get("metadata", {}).get("cancel_reason") == "cap"
               except (json.JSONDecodeError, OSError):
                   pass
       if cap_killed:
           from automil.cells.reconcile import reconcile_budget_kill
           expected_folds = self._read_fold_count_for_node(node_id)
           payload = reconcile_budget_kill(
               node_id=node_id,
               archive_dir=self.archive_dir,
               graph=self.graph,
               expected_fold_count=expected_folds,
           )
           # Reconcile_budget_kill writes result.json + budget_killed flag. NOW we own the graph mutation:
           if payload.get("partial_folds", 0) >= 1:
               self.graph.add_executed(
                   node_id=node_id,
                   composite=payload["composite"],
                   metrics=payload.get("metrics", {}),
                   status="keep",  # Pareto cascade decides keep vs discard via _reevaluate_descendants
               )
               # tag the graph entry with budget_killed for operator visibility
               try:
                   gnode = self.graph.get_node(node_id)
                   gnode.setdefault("metadata", {})["budget_killed"] = True
                   self.graph.save()
               except (AttributeError, KeyError):
                   logger.warning("Could not tag node %s with budget_killed; graph schema differs", node_id)
           else:
               self.graph.mark_failed(node_id=node_id, status="crash")
               try:
                   gnode = self.graph.get_node(node_id)
                   gnode.setdefault("metadata", {})["budget_killed"] = True
                   self.graph.save()
               except (AttributeError, KeyError):
                   pass
           logger.info(
               "Cap-driven cancel reconciled for %s: status=%s composite=%.4f partial_folds=%d/%d",
               node_id, payload["status"], payload["composite"],
               payload["partial_folds"], payload["expected_folds"],
           )
           return  # do NOT fall through to the standard completion path

       # ... (existing logic continues unchanged)

       Add a `_read_fold_count_for_node(self, node_id)` helper too:

       def _read_fold_count_for_node(self, node_id: str) -> int:
           """Read AUTOMIL_FOLD_COUNT-equivalent from the spec or fall back to config."""
           # Try the spec first
           for path in (self.running_dir / f"{node_id}.json",
                        self.archive_dir / node_id / "spec.json"):
               if path.exists():
                   try:
                       spec = json.loads(path.read_text())
                       env = (spec.get("env") or {}) if isinstance(spec, dict) else {}
                       if "AUTOMIL_FOLD_COUNT" in env:
                           return int(env["AUTOMIL_FOLD_COUNT"])
                   except (json.JSONDecodeError, OSError, ValueError, TypeError):
                       continue
           # Fall back: read automil/config.yaml:training.fold_count
           try:
               import yaml
               from automil.cli._helpers import _find_automil_dir
               cfg = yaml.safe_load((_find_automil_dir() / "config.yaml").read_text())
               return int((cfg.get("training") or {}).get("fold_count", 5))
           except Exception:
               return 5

       NOTE: the EXACT integration points depend on how _handle_completion is structured today. The implementer MUST read the current method first and place the cap-detection block at the SAME logical point as the existing "did it crash?" branch — i.e. AFTER returncode is known, BEFORE the standard archive/graph update path. If _handle_completion has multiple branches (success/crash/cancel), insert the cap detection at the top of the "cancel" branch — the cap-cancel path replaces, NOT supplements, the standard cancel reconciliation.

    5) Inject AUTOMIL_FOLD_COUNT into experiment subprocess env. Find `_build_subprocess_env` (or wherever env is constructed before subprocess.Popen). Add:

       # Phase 4 (D-120): inject fold count so SIGTERM handler in train script can read it
       try:
           import yaml
           from automil.cli._helpers import _find_automil_dir
           cfg = yaml.safe_load((_find_automil_dir() / "config.yaml").read_text())
           fold_count = int((cfg.get("training") or {}).get("fold_count", 5))
       except Exception:
           fold_count = 5
       env["AUTOMIL_FOLD_COUNT"] = str(fold_count)

       (Use whatever variable name the env-building function uses for the env dict — likely `env` or `subprocess_env`.)

    Run the existing test suite to confirm no regression:
    `uv run pytest tests/test_orchestrator_pid_starttime.py tests/test_orchestrator_dotenv.py tests/test_orchestrator_env_whitelist.py tests/test_integration.py tests/backends/ -x`
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator_pid_starttime.py tests/test_orchestrator_dotenv.py tests/test_orchestrator_env_whitelist.py tests/test_integration.py tests/backends/ -x 2>&1 | tail -20 && grep -c "def _tick_cells" src/automil/backends/_orchestrator_daemon.py && grep -c "def _running_in_cell" src/automil/backends/_orchestrator_daemon.py && grep -c "self._tick_cells()" src/automil/backends/_orchestrator_daemon.py && grep -c "AUTOMIL_FOLD_COUNT" src/automil/backends/_orchestrator_daemon.py && grep -c 'cancel_reason.*=.*"cap"' src/automil/backends/_orchestrator_daemon.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _tick_cells" src/automil/backends/_orchestrator_daemon.py` returns 1.
    - `grep -c "def _running_in_cell" src/automil/backends/_orchestrator_daemon.py` returns 1.
    - `grep -c "self._tick_cells()" src/automil/backends/_orchestrator_daemon.py` returns 1.
    - `grep -c "AUTOMIL_FOLD_COUNT" src/automil/backends/_orchestrator_daemon.py` returns at least 2 (env injection + helper).
    - `grep -c 'cancel_reason.*=.*"cap"' src/automil/backends/_orchestrator_daemon.py` returns at least 1.
    - `grep -c "from automil.cells import list_cells" src/automil/backends/_orchestrator_daemon.py` returns at least 1 (or via the import in _tick_cells body).
    - `grep -c "reconcile_budget_kill" src/automil/backends/_orchestrator_daemon.py` returns at least 1.
    - `grep -c "self.backend.cancel(handle" src/automil/backends/_orchestrator_daemon.py` returns at least 1.
    - The annotation of `cancel_reason='cap'` MUST appear LEXICALLY BEFORE `backend.cancel(handle` in `_tick_cells` body. Verify with `awk '/_tick_cells/,/def [a-z_]/'  src/automil/backends/_orchestrator_daemon.py` and inspect — record line numbers.
    - `uv run pytest tests/test_orchestrator_pid_starttime.py tests/test_orchestrator_dotenv.py tests/test_orchestrator_env_whitelist.py tests/test_integration.py tests/backends/ -x` PASSES (existing daemon tests preserved).
    - `uv run pytest tests/ -x` passes (existing 558+9 baseline).
  </acceptance_criteria>
  <done>_tick_cells inserted into tick(); _running_in_cell helper present; cap-cancel path wires through reconcile_budget_kill + graph mutations; AUTOMIL_FOLD_COUNT injected into env; existing tests preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_tick_cells.py (daemon-level integration tests)</name>
  <files>tests/test_tick_cells.py</files>
  <read_first>
    - src/automil/backends/_orchestrator_daemon.py (just-modified — for the exact API of ExperimentOrchestrator: __init__, tick, _tick_cells, _running_in_cell, _handle_completion)
    - tests/backends/conftest.py (for MockSLURMBackend / make_spec patterns; tests use MockSLURM as the backend stand-in to avoid real subprocess)
    - tests/test_orchestrator_pid_starttime.py (read for daemon construction + tmp_path setup pattern)
    - src/automil/cells/state.py + registry.py (for write_cell + Cell + make_cell_id)
  </read_first>
  <action>
    Create `tests/test_tick_cells.py`. Each test constructs an ExperimentOrchestrator pointed at a tmp_path automil overlay, populates `automil/cells/<id>.json` directly, populates `running/<node>.json` with metadata.cell_id matching, then invokes `daemon._tick_cells()` and asserts state.

    Required tests:

    1. test_tick_cells_active_to_refusing_new:
       - Write a Cell with status=ACTIVE, started_at = time.time() - (21600 - 1800 + 1) (so remaining ~ 1799 < buffer 1800).
       - No running experiments.
       - Call `daemon._tick_cells()`.
       - Re-read `cells/<id>.json`; assert status == "refusing-new".
       - Assert backend.cancel was NOT called (no in-cell experiments).

    2. test_tick_cells_terminating_fires_cancel_with_cap_reason:
       - Write a Cell with status=REFUSING_NEW, started_at = time.time() - 22000 (remaining < 0).
       - Populate self.running with a fake RunningExperiment whose spec has metadata.cell_id matching, and write running/<node>.json on disk.
       - Use a MockSLURMBackend or a Mock that records cancel calls.
       - Call `daemon._tick_cells()`.
       - Assert: cells/<id>.json status == "terminating".
       - Assert: running/<node>.json now has metadata.cancel_reason == "cap".
       - Assert: backend.cancel was called once with the expected handle.
       - CRITICAL: assert that the running/<node>.json file was modified BEFORE backend.cancel was called (use a Mock with side_effect that captures the running/<node>.json contents at call time, OR use a backend.cancel that monkeypatches and reads the file inside the cancel callback).

    3. test_tick_cells_finalized_when_running_empty:
       - Write Cell with status=TERMINATING.
       - self.running is empty.
       - Call _tick_cells.
       - Assert status == "finalized".
       - Assert backend.cancel NOT called.

    4. test_tick_cells_idempotent_on_finalized:
       - Status=FINALIZED, call _tick_cells twice.
       - Assert no transition occurred either time; no cancels fired.

    5. test_running_in_cell_filters_by_metadata_cell_id:
       - Populate self.running with 3 experiments: 2 with metadata.cell_id == "abc", 1 with cell_id == "xyz", 1 with no cell_id (legacy).
       - Call _running_in_cell("abc").
       - Assert returns 2 handles (just the two matching).

    6. test_handle_completion_with_cap_cancel_reason_calls_reconcile:
       - Set up a running spec on disk with metadata.cancel_reason="cap".
       - Pre-populate archive/<node>/fold_0_result.json + fold_1_result.json (partial 2-of-5 case).
       - Call daemon._handle_completion(node_id, returncode=0).
       - Assert: archive/<node>/result.json exists with status="partial", partial_folds=2, metadata.budget_killed=True.
       - Assert: graph node has metadata.budget_killed=True. Inspect via daemon.graph.get_node(node_id) or by reading graph.json.

    7. test_handle_completion_with_cap_cancel_zero_folds_marks_crash:
       - Same setup but no fold files.
       - Assert: archive/<node>/result.json with status="crashed", metadata.budget_killed=True.
       - Assert: graph node status == "crash".

    8. test_automil_fold_count_injected_into_subprocess_env:
       - Write automil/config.yaml with `training: {fold_count: 7}`.
       - Construct daemon, call its env-building helper (whatever it is named — read source).
       - Assert env["AUTOMIL_FOLD_COUNT"] == "7".

    Use `unittest.mock.MagicMock` for the backend if MockSLURMBackend is not convenient for `cancel`-call counting. Reuse fixtures from tests/backends/conftest.py where possible.

    Mocking strategy for backend.cancel order-of-operation (Test 2): give the Mock a `side_effect` callable that captures `(self.running_dir / f"{handle.node_id}.json").read_text()` into a list; then assert the captured spec already has `cancel_reason: "cap"` at cancel time.

    Run pytest and verify GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tick_cells.py -xvs 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_tick_cells.py` exists.
    - `grep -c "def test_" tests/test_tick_cells.py` returns at least 8.
    - `grep -c "_tick_cells" tests/test_tick_cells.py` returns at least 4.
    - `grep -c "_running_in_cell\|running_in_cell" tests/test_tick_cells.py` returns at least 1.
    - `grep -c "cancel_reason" tests/test_tick_cells.py` returns at least 2.
    - `grep -c "budget_killed" tests/test_tick_cells.py` returns at least 2.
    - `grep -c "AUTOMIL_FOLD_COUNT" tests/test_tick_cells.py` returns at least 1.
    - `grep -c "MagicMock\|MockSLURMBackend\|Mock(" tests/test_tick_cells.py` returns at least 1 (backend mock used).
    - `uv run pytest tests/test_tick_cells.py -x` PASSES (all 8 tests green).
    - `uv run pytest tests/ -x` passes (existing baseline).
  </acceptance_criteria>
  <done>Daemon-level integration tests cover: ACTIVE->REFUSING_NEW, REFUSING_NEW->TERMINATING with cancel-and-annotate, TERMINATING->FINALIZED, _running_in_cell filtering, _handle_completion cap-detection with partial fold reconciliation, zero-fold crash path, AUTOMIL_FOLD_COUNT env injection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon -> backend.cancel(SIGTERM) | Daemon trusts backend to deliver SIGTERM to process group (D-115); LocalBackend's 30s grace + SIGKILL chain is the existing contract. |
| Daemon -> running/<node>.json (cancel_reason annotation) | Race: daemon writes annotation, then calls backend.cancel which may return before the SIGTERM handler in the child fires. Annotation must be written FIRST so reconcile_budget_kill can detect cap kill regardless of timing. |
| Subprocess env injection | AUTOMIL_FOLD_COUNT is non-secret; trusted source (config.yaml). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering | cancel_reason annotation race (cancel returns before annotation lands) | mitigate | Code structure: annotation write BEFORE backend.cancel call inside _tick_cells. Test 2 enforces ordering by capturing spec contents inside the Mock side_effect. |
| T-04-19 | Information Disclosure | metadata.budget_killed on graph node | accept | Operator-facing flag; non-secret. Same exposure as existing metadata fields. |
| T-04-20 | Denial of Service | _tick_cells iterating thousands of cells per tick | accept | Phase 4 scope: ≤ ~50 cells expected (one per (dataset, encoder, parent) tuple Leo cares about); next_status is O(1) per cell so worst case ~50 ms per tick. v2 GC (D-127) addresses long-term. |
</threat_model>

<verification>
- `uv run pytest tests/test_tick_cells.py -x` passes all 8 tests.
- `uv run pytest tests/ -x` passes (existing baseline + new tests).
- `grep -c "def _tick_cells" src/automil/backends/_orchestrator_daemon.py` returns 1.
- `grep -c "self._tick_cells()" src/automil/backends/_orchestrator_daemon.py` returns 1.
- `grep -c 'cancel_reason.*=.*"cap"' src/automil/backends/_orchestrator_daemon.py` returns at least 1.
- `grep -c "AUTOMIL_FOLD_COUNT" src/automil/backends/_orchestrator_daemon.py` returns at least 2.
</verification>

<success_criteria>
1. Daemon `tick()` invokes `_tick_cells()` once per cycle.
2. ACTIVE -> REFUSING_NEW transitions persist with no cancel.
3. REFUSING_NEW -> TERMINATING transitions annotate `cancel_reason='cap'` BEFORE invoking `backend.cancel(handle, SIGTERM)`.
4. `_handle_completion` detects cap-driven cancels and routes through `reconcile_budget_kill` to upgrade the graph node to `executed` (partial>=1) or `crash` (zero folds), tagging `metadata.budget_killed=True`.
5. AUTOMIL_FOLD_COUNT is injected into experiment subprocess env so the SIGTERM handler can read it.
6. Existing 558+9 baseline still green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-6h-per-cell-hard-cap-cell-concept-formalisation/04-07-SUMMARY.md` per the standard summary template.
</output>
